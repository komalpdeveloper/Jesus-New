import 'package:flutter/material.dart';

// Brand palette (chromatic colors limited strictly to these four + neutrals)
const Color kDeepBlack = Color(0xFF05070A); // neutral background (allowed neutral)
const Color kPurple = Color(0xFF6C2BD9); // Brand Purple
const Color kRoyalBlue = Color(0xFF0B1A3A); // Brand Royal Blue (deep variant)
const Color kGold = Color(0xFFFFD700); // Brand Gold
const Color kRed = Color(0xFFC62828); // Brand Red

// All previous neon/legacy colors removed to enforce strict 4-color brand palette.
